---
title: "What is Dacha"
description: "An overview of the Dacha game engine, what it does, and how you work with it."
---

import { Image } from 'astro:assets';
import { LinkCard } from '@astrojs/starlight/components';

import editorShot from '../../../assets/editor/editor-overview.png';

Dacha is a game engine for 2D browser games, with a visual editor. It has two parts, and you use both:

- **`dacha`** — the engine. An npm package your game imports.
- **`dacha-workbench`** — the editor. A desktop application you point at your project.

Your game is an ordinary web application: you add the engine as a dependency and build it with a bundler of your choice.

## The game is data

A Dacha game is not defined by a startup script that constructs its world. The game itself is a description: its scenes, actors, components, templates, systems and configuration are all data. The engine reads that description at startup and turns it into a running world.

This description is stored as JSON in your project, and you are not expected to write it by hand. The editor does.

## The editor is a separate application

You launch the editor yourself, and it reads your project:

- **the configuration**, so you build scenes by dragging actors around;
- **your assets directory**, so an image you drop in becomes selectable straight away;
- **your own components and systems**, so they show up in the inspector with their fields
  laid out, alongside the built-in ones.

That last one is what makes the editor useful on a real project rather than on a demo, and
it needs no wiring from you.

It writes to your project too: ask it for a new component, system or behavior and the file
lands in your source, already visible to both the game and the inspector.

<figure class="screenshot">
  <Image
    src={editorShot}
    alt="The editor: the scene tree on the left listing camera, player and background; the game's grass field with the player sprite in the viewport; the inspector on the right showing the player's Animatable, Keyboard Control, Movement, Sprite and Transform components."
  />
  <figcaption>
    The editor: the scene tree, the viewport, and the inspector for the selected actor.
    `Movement` there is a component from the game's own source, listed among the engine's
    without any distinction.
  </figcaption>
</figure>

<LinkCard
  title="Scripts & auto-registration"
  description="How a class you write becomes a class the editor knows about."
  href="/writing-game-code/auto-registration/"
/>

## What comes with it

- **Built-in systems** for rendering, physics, character movement, interpolation,
  animation, audio, keyboard and mouse input, cameras, behaviors and mounting a user
  interface.
- **Built-in components** for them to read — transforms, sprites, meshes, text, shapes,
  colliders, rigid and character bodies, cameras, audio sources and the rest.

Nothing about them is privileged: they are the same kind of class you write yourself.
Enable the systems you need, ignore the others, and add your own alongside them.

## Where it fits

Dacha suits 2D browser games where you want a visual scene editor without adopting a large
engine, and where the game logic is comfortable in TypeScript. It has been used to build
[games for game jams](/resources/examples/), which is the workload it is shaped around:
small teams, short timelines, and a browser as the target.

## When not to use it

- **It is 2D only.** There is no 3D pipeline and none is planned.
- **The target is the browser.** There is no console export and no native desktop or
  mobile packaging, though browser games do run on mobile.
- **It is a small project.** There is no asset store, no plugin marketplace, and no
  commercial support.

If any of those is a requirement, a larger engine will serve you better.

:::caution[Before version 1.0]
Breaking changes land between minor versions. There are
[migration notes](/reference/migration/) for each one, but you should expect to read them.
:::

If none of that rules it out, [installation & setup](/getting-started/installation/) gets a
project running in a few minutes.
